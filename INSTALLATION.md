# 🚀 دليل التثبيت والتشغيل

## المتطلبات الأساسية

### ✅ الخيار الأول: استخدام Docker (الموصى به)

```bash
# 1. تثبيت Docker Desktop من:
# https://www.docker.com/products/docker-desktop

# 2. التحقق من التثبيت
docker --version
docker-compose --version
```

### ✅ الخيار الثاني: التثبيت المحلي

- Node.js v18+ من https://nodejs.org/
- MySQL 8.0+ من https://www.mysql.com/downloads/
- npm (يأتي مع Node.js)

---

## 🐳 التشغيل مع Docker (الطريقة الأسهل)

### الخطوة 1: فتح PowerShell وانتقل للمجلد

```powershell
cd C:\Users\user\Desktop\casher
```

### الخطوة 2: بناء وتشغيل الحاويات

```powershell
# بناء الصور وتشغيل الحاويات
docker-compose up --build

# أو في الخلفية
docker-compose up -d --build
```

### الخطوة 3: الوصول للتطبيق

بعد اكتمال البناء (قد يستغرق 2-3 دقائق)، افتح متصفحك:

```
Frontend:  http://localhost:3001
Backend:   http://localhost:3000/api
```

### الخطوة 4: تسجيل الدخول

```
البريد: admin@mirmaia.com
كلمة المرور: [لم تُحدد بعد - استخدم migrate التلقائي]
```

---

## 🖥️ التشغيل المحلي بدون Docker

### الخطوة 1: إعداد قاعدة البيانات MySQL

```powershell
# 1. فتح MySQL
mysql -u root -p

# 2. تشغيل الأوامر التالية:
CREATE DATABASE mirmaia_pos;
CREATE USER 'mirmaia_user'@'localhost' IDENTIFIED BY 'mirmaia_pass123';
GRANT ALL PRIVILEGES ON mirmaia_pos.* TO 'mirmaia_user'@'localhost';
FLUSH PRIVILEGES;

# 3. استيراد البيانات الأولية
mysql -u mirmaia_user -p mirmaia_pos < backend/src/database/init.sql
```

### الخطوة 2: تشغيل Backend

```powershell
cd backend
npm install
npm run build
npm start
```

يجب أن ترى: `✅ Server running on port 3000`

### الخطوة 3: تشغيل Frontend (في نافذة جديدة)

```powershell
cd frontend
npm install
npm start
```

سيفتح المتصفح تلقائياً على http://localhost:3000

---

## 🛑 إيقاف التطبيق

### مع Docker:

```powershell
# إيقاف الخدمات
docker-compose down

# مع حذف البيانات
docker-compose down -v
```

### بدون Docker:

```
اضغط Ctrl+C في كلا نافذتي PowerShell
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: المنفذ 3000 أو 3001 مستخدم بالفعل

```powershell
# البحث عن العملية التي تستخدم المنفذ
Get-Process | Where-Object {$_.ProcessName -like "*node*"}

# أو استخدام منفذ مختلف في docker-compose.yml
```

### المشكلة: خطأ قاعدة البيانات

```powershell
# تحقق من أن MySQL يعمل
mysql -u root -p -e "SELECT VERSION();"

# تحقق من الاتصال
docker-compose logs mysql
```

### المشكلة: خطأ CORS

تأكد من أن `REACT_APP_API_URL` صحيح في frontend/.env

---

## 🔄 أوامر مفيدة

### Docker

```powershell
# عرض جميع الحاويات
docker-compose ps

# عرض السجلات
docker-compose logs -f backend
docker-compose logs -f mysql

# إعادة بناء صورة معينة
docker-compose up --build backend
```

### npm

```powershell
# حذف node_modules
rm -r node_modules

# إعادة تثبيت الحزم
npm install

# تشغيل في وضع التطوير مع المراقبة
npm run dev
```

---

## ✅ تحقق من التثبيت الناجح

1. **Frontend يحمل**:
   - صفحة تسجيل الدخول تظهر بصحة

2. **Backend يستجيب**:
   ```powershell
   curl http://localhost:3000/api/health
   ```
   يجب أن يرد: `{"status":"OK"}`

3. **قاعدة البيانات متصلة**:
   - لا توجد أخطاء في سجلات الخادم

---

## 📱 استخدام التطبيق

### أول مرة:

1. سجل الدخول بـ:
   - البريد: `admin@mirmaia.com`
   - كلمة المرور: [ستُعيّن في البيئة]

2. أضف المنتجات من قائمة "المنتجات"

3. استخدم "الكاشير" للبيع

4. اعرض "التقارير" والإحصائيات

---

## 🎯 الخطوات التالية

بعد التشغيل الناجح:

1. [ ] أضف منتجاتك إلى القائمة
2. [ ] أنشئ حسابات للموظفين
3. [ ] عدّل الإعدادات حسب احتياجاتك
4. [ ] جرّب الكاشير والتقارير
5. [ ] استعد للنشر على السيرفر

---

**هل لديك مشكلة؟** تحقق من ملف README.md الرئيسي للمزيد من المساعدة.
