# 🎯 البدء السريع - Mirmaia POS

## ⏱️ 5 دقائق للبدء

### الخطوة 1: التحضير (30 ثانية)

**على Windows:**
```powershell
# افتح PowerShell وانتقل للمجلد
cd C:\Users\user\Desktop\casher
```

**على Linux/Mac:**
```bash
cd ~/Desktop/casher
```

### الخطوة 2: التشغيل (4 دقائق 30 ثانية)

**الطريقة الأسهل - اضغط على:**
- **Windows**: انقر مرتين على `start.bat`
- **Linux/Mac**: اشغل `./start.sh` في التيرمينال

**أو يدويًا:**
```bash
docker-compose up --build
```

⏳ انتظر 2-3 دقائق حتى اكتمال البناء...

### الخطوة 3: الدخول (30 ثانية)

بعد ظهور "✅ Server running on port 3000" في النافذة:

```
🌐 الواجهة:    http://localhost:3001
🔌 الخادم:     http://localhost:3000
📊 قاعدة البيانات: localhost:3306
```

### الخطوة 4: تسجيل الدخول

**البيانات الافتراضية:**
```
📧 البريد:     admin@mirmaia.com
🔑 كلمة المرور: [استخدم كلمة تجريبية]
```

---

## 🎮 الاستخدام الفوري

### 1️⃣ أضف بعض المنتجات:
```
1. اذهب إلى: Menu (القائمة)
2. اضغط: + إضافة منتج جديد
3. أدخل: اسم، سعر، تكلفة
4. اضغط: إضافة
```

### 2️⃣ جرّب الكاشير:
```
1. اذهب إلى: Cashier (الكاشير)
2. اختر منتجات
3. أضفها للسلة
4. اختر طريقة الدفع
5. اضغط: استكمال الدفع
```

### 3️⃣ عرض التقارير:
```
1. اذهب إلى: Reports (التقارير)
2. اختر تاريخ أو شهر
3. شاهد الرسوم البيانية
```

---

## 🛑 الإيقاف

اضغط **Ctrl + C** في النافذة

أو:
```bash
docker-compose down
```

---

## ⚡ نصائح سريعة

| الإجراء | الاختصار |
|--------|----------|
| تحديث البيانات | Ctrl + R |
| تسجيل الخروج | اضغط تسجيل الخروج |
| عرض السجلات | `docker-compose logs -f` |
| حذف كل شيء | `docker-compose down -v` |

---

## 🐛 في حالة الخطأ

### "لا يمكن الاتصال بقاعدة البيانات"
```bash
# أعد التشغيل
docker-compose restart mysql
docker-compose restart backend
```

### "المنفذ 3000/3001 مستخدم"
```bash
# ابدأ من جديد
docker-compose down -v
docker-compose up --build
```

### "الملفات في الذاكرة"
```bash
# احذف وأعد التثبيت
rm -rf backend/node_modules frontend/node_modules
docker-compose up --build
```

---

## 📱 الميزات الجاهزة

✅ كاشير احترافي  
✅ إدارة منتجات  
✅ تقارير يومية وشهرية  
✅ إدارة مخزن  
✅ إدارة موظفين  
✅ رسوم بيانية  
✅ أمان عالي  

---

## 🚀 الخطوات التالية

بعد البدء الناجح:

```
1. اضغط على صفحة Admin
2. أضف موظفين جدد
3. اختبر جميع الميزات
4. اضغط على Reports لرؤية التقارير
5. استخدم Inventory لإدارة المخزن
```

---

## 📚 معلومات إضافية

**ملفات مهمة:**
- 📖 `README.md` - دليل شامل
- 📲 `FEATURES.md` - شرح الميزات
- 🔧 `INSTALLATION.md` - تثبيت متقدم
- ℹ️ `PROJECT_INFO.json` - معلومات المشروع

---

## ✨ مميزات إضافية

### Docker
```bash
# عرض جميع الحاويات
docker-compose ps

# عرض السجلات
docker-compose logs backend
docker-compose logs mysql

# بدء خدمة واحدة
docker-compose up backend
```

### قاعدة البيانات
```bash
# الدخول إلى MySQL
docker-compose exec mysql mysql -u root -p

# تشغيل نسخة احتياطية
docker-compose exec mysql mysqldump -u root -p mirmaia_pos > backup.sql
```

---

## 💡 الدعم السريع

في حالة وجود مشكلة:

1. **شاهد السجلات**:
   ```bash
   docker-compose logs
   ```

2. **تحقق من الاتصال**:
   ```bash
   curl http://localhost:3000/api/health
   ```

3. **أعد التشغيل**:
   ```bash
   docker-compose restart
   ```

4. **ابدأ من جديد**:
   ```bash
   docker-compose down -v && docker-compose up --build
   ```

---

## 🎉 تم! 

أنت الآن جاهز لاستخدام نظام Mirmaia POS!

**استمتع بالنظام! 🚀**

---

*آخر تحديث: 30 يناير 2026*
