# 🎯 Mirmaia Coffee Shop - نظام كاشير متكامل

نظام كاشير احترافي وشامل لإدارة قهوة Mirmaia، مصمم باستخدام أحدث التقنيات والممارسات الاحترافية.

## ✨ المميزات

### 🧾 نظام الكاشير
- واجهة كاشير احترافية وسهلة الاستخدام
- إضافة منتجات للسلة مع حساب تلقائي للإجمالي
- دعم الخصم والضريبة (15% VAT)
- دعم طرق دفع متعددة (نقد، بطاقة، كلاهما)
- طباعة الفاتورة
- تتبع الكاشيرين

### 📋 إدارة القائمة (المنيو)
- إضافة وتعديل وحذف المنتجات
- تصنيف المنتجات حسب الفئات
- عرض الأسعار وأسعار التكلفة
- تفعيل/تعطيل المنتجات

### 📦 نظام إدارة المخزن
- متابعة شاملة لمستويات المخزن
- تنبيهات تلقائية عند انخفاض المخزون
- تسجيل حركات المخزن (شراء، بيع، إرجاع، تعديل)
- سجل تاريخي كامل لتحركات المخزن
- تحديد حد أدنى وأقصى لكل منتج

### 📊 التقارير والإحصائيات
- تقارير يومية تفصيلية للمبيعات
- تقارير شهرية شاملة
- رسوم بيانية تفاعلية لتحليل المبيعات
- توزيع المبيعات حسب طرق الدفع
- أفضل المنتجات مبيعاً
- تحليل المبيعات حسب الفئات

### 👥 نظام إدارة المستخدمين
- ثلاث مستويات من الصلاحيات:
  - **مسؤول (Admin)**: الوصول الكامل لجميع الميزات
  - **موظف (Staff)**: عرض التقارير والمخزن
  - **كاشير (Cashier)**: استخدام الكاشير فقط
- إدارة المستخدمين وتفعيل/تعطيل الحسابات
- تغيير كلمات المرور

### 🔐 الأمان
- نظام مصادقة آمن باستخدام JWT Tokens
- تشفير كلمات المرور
- التحقق من الصلاحيات على جميع العمليات

## 🛠️ التكنولوجيات المستخدمة

### Backend
- **Node.js + Express**: خادم API قوي وموثوق
- **TypeScript**: للكتابة الآمنة والموثوقة
- **MySQL**: قاعدة بيانات علائقية قوية
- **JWT**: للمصادقة الآمنة
- **bcryptjs**: لتشفير كلمات المرور

### Frontend
- **React 18**: مكتبة واجهات المستخدم الحديثة
- **TypeScript**: للتطوير الآمن
- **React Router**: للملاحة بين الصفحات
- **Zustand**: لإدارة الحالة
- **Recharts**: لرسم الرسوم البيانية
- **Axios**: لاستدعاء API

### DevOps
- **Docker**: لتسهيل النشر والتوسع
- **Docker Compose**: لتنظيم الخدمات
- **MySQL 8.0**: قاعدة البيانات

## 📋 متطلبات التثبيت

- Docker و Docker Compose
- أو Node.js v18+ و MySQL 8.0+

## 🚀 البدء السريع

### مع Docker (الطريقة الموصى بها) — كل العمل على Docker

تشغيل **جميع الخدمات** بأمر واحد: MySQL + Backend + Frontend + phpMyAdmin.

```bash
# 1. انسخ المشروع
git clone <repo-url>
cd mirmaia1-master

# 2. بناء وتشغيل كل الحاويات
docker-compose up -d --build

# 3. الوصول
# Frontend:   http://localhost:3001
# Backend:    http://localhost:3000/api
# phpMyAdmin: http://localhost:8081
# MySQL:      localhost:3306
```

**أوامر مفيدة:**
- إيقاف كل الخدمات: `docker-compose down`
- عرض السجلات: `docker-compose logs -f`
- إعادة البناء: `docker-compose up -d --build`

### بدون Docker (التطوير المحلي)

#### تثبيت Backend

```bash
cd backend
npm install
npm run build
npm start
```

#### تثبيت Frontend

```bash
cd frontend
npm install
npm start
```

## 🔑 بيانات المستخدم الافتراضية

```
admin@mirmaia.com  → كلمة المرور: admin123  (مسؤول)
admin@admin.com    → كلمة المرور: admin     (مسؤول)
```

إذا كانت القاعدة موجودة مسبقاً، نفّذ `backend/src/database/update-admin-password.sql` في phpMyAdmin أو عبر Docker لإضافة/تحديث الحسابات.

## 📱 واجهات التطبيق

### صفحة تسجيل الدخول
- واجهة آمنة وموثوقة
- تحقق من البيانات

### الكاشير
- عرض المنتجات حسب الفئات
- إضافة للسلة بسهولة
- حساب الإجمالي التلقائي
- اختيار طريقة الدفع
- تطبيق خصومات

### التقارير
- تقرير يومي شامل
- تقرير شهري مفصل
- رسوم بيانية تفاعلية
- تصدير البيانات

### المخزن
- عرض جميع المنتجات ومستوياتها
- تنبيهات المخزون المنخفض
- تعديل كميات المخزن
- سجل تاريخي

## 📊 هيكل قاعدة البيانات

```
users              - جدول المستخدمين
products           - جدول المنتجات
categories         - فئات المنتجات
inventory          - مستويات المخزن
inventory_logs     - سجل تحركات المخزن
orders             - جدول الطلبات
order_items        - تفاصيل الطلبات
daily_reports      - التقارير اليومية
monthly_reports    - التقارير الشهرية
settings           - إعدادات النظام
```

## 🔄 API Endpoints

### المصادقة
- `POST /api/auth/login` - تسجيل الدخول
- `POST /api/auth/register` - إنشاء حساب
- `POST /api/auth/verify` - التحقق من الرمز

### المنتجات
- `GET /api/products` - جميع المنتجات
- `POST /api/products` - إضافة منتج (Admin)
- `PUT /api/products/:id` - تعديل منتج (Admin)
- `DELETE /api/products/:id` - حذف منتج (Admin)

### الطلبات
- `POST /api/orders` - إنشاء طلب
- `GET /api/orders` - عرض الطلبات
- `GET /api/orders/:id` - تفاصيل الطلب

### المخزن
- `GET /api/inventory` - عرض المخزن
- `POST /api/inventory/adjust` - تعديل المخزن
- `GET /api/inventory/logs/history` - سجل التحركات

### التقارير
- `GET /api/reports/daily` - التقرير اليومي
- `GET /api/reports/monthly` - التقرير الشهري
- `GET /api/reports/sales/by-category` - المبيعات حسب الفئة

### المستخدمين
- `GET /api/users` - جميع المستخدمين (Admin)
- `POST /api/users` - إضافة مستخدم (Admin)
- `PUT /api/users/:id` - تعديل مستخدم
- `POST /api/users/:id/deactivate` - تعطيل مستخدم (Admin)

## 🔧 إعدادات البيئة

### Backend (.env)

```env
DB_HOST=mysql
DB_USER=mirmaia_user
DB_PASSWORD=mirmaia_pass123
DB_NAME=mirmaia_pos
JWT_SECRET=your_super_secret_jwt_key
PORT=3000
NODE_ENV=production
```

### Frontend (.env)

```env
REACT_APP_API_URL=http://localhost:3000/api
```

## 🐛 حل المشاكل الشائعة

### المشكلة: لا يمكن الاتصال بـ MySQL
**الحل**: تأكد من أن حاوية MySQL قيد التشغيل:
```bash
docker-compose logs mysql
```

### المشكلة: خطأ في تثبيت الحزم
**الحل**: احذف مجلد node_modules و package-lock.json وأعد التثبيت:
```bash
rm -rf node_modules package-lock.json
npm install
```

## 📝 الترخيص

هذا المشروع مخصص لاستخدام Mirmaia Coffee Shop.

## 📞 الدعم والتطوير المستقبلي

### المميزات المخطط إضافتها:
- ✅ نظام الفواتير الإلكترونية (E-Invoice)
- ✅ تطبيق موبايل
- ✅ نظام الولاء والعروض
- ✅ التكامل مع أنظمة الدفع الإلكتروني
- ✅ نظام الحجز والطلب المسبق
- ✅ تحليلات متقدمة

---

**الإصدار**: 1.0.0  
**آخر تحديث**: 30 يناير 2026  
**المطورين**: فريق تطوير Mirmaia
